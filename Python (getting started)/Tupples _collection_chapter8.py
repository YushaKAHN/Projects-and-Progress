# tupples are like list but are not modifiable,take up less storage.
# non mutablility


# sorted list of tupples:
# lst = []
# for key, val in counts.items():
# newtup = (val, key)
# lst.append(newtup)
# lst = sorted(lst, reverse=True)
# print(lst
